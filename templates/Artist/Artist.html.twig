{% extends 'base.html.twig' %}

{% block title %}Hello ArtistController!
{% endblock %}

{% block body %}
<!--{{ dump(categories) }}-->
    
    <div class="text-center my-5">
        {% for category in categories %}
            <a href="" role="button" class="btn btn-outline-primary"> {{ category.name }} </a>
        {% endfor %}
    </div>
    <div class="mb-5 row">
        {% for artist in artists %}
            <div class="px-3 col-4 ">
                <div class="card border-primary my-3">
                    <div class="card-header text-end"><span class="badge rounded-pill bg-secondary">Concert</span></div>
                    <div class="card-body">
                        <h4 class="card-title">
                            Artiste : {{ artist.name }}</h4>
                        <p class="card-text"> Style musical :
                            {{ artist.description }}
                            ...</p>
                        <p>
                            <a href="">Découvrir l'artiste</a>
                        </p>
                    </div>
                </div>
            </div> 

        {% endfor %} 
    </div>
    {% endblock %}